{
    "type": "View?class=box-shadow flex column;id=droplist;style:[width=fit-content;transition=opacity .1s, transform .1s;height=fit-content;overflowY=auto;maxWidth=40rem;transform=scale(0.5);opacity=0;pointerEvents=none;position=fixed;padding=.2rem;borderRadius=.5rem;backgroundColor=#fff;zIndex=999]",
    "controls": [{
        "event": "click:body?)(:droplist-timer=timer():[():[)(:droplist-positioner].click()]:400?)(:droplist-positioner;!)(:clickedElement.id.isChildOfId():[)(:droplist-positioner];!)(:clickedElement.id.isChildOfId():droplist"
    }, {
        "event": "mouseleave?)(:droplist-timer=timer():[():[)(:droplist-positioner].click()]:400"
    }, {
        "event": "mouseenter?clearTimer():[)(:droplist-timer]"
    }]
}